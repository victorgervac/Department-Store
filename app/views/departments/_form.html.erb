<h1><%= @department ? "Edit Department" : "Create Department" %></h1>
<hr />
<%= form_for @department do |f| %>
    <%= f.label :name %>
    <%= f.text_field :name %>
    
    <%= f.submit %>
    <% end %>